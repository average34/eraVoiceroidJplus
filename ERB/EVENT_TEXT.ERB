;旧EVENT3.ERB
@TEXT
SIF FLAG:6 == 0
	RETURN 0
;--------------------------------------------------------
;愛撫
;--------------------------------------------------------
IF SELECTCOM == 0
	A = UP:0
	B = UP:14
	C = A+B

	IF C < 100
		IF TALENT:11
			PRINTFORML %CALLNAME:TARGET%は愛撫されながらも反抗的な態度を取っている。
		ELSE
			PRINTFORML %CALLNAME:TARGET%はむずかゆそうに身体をよじるが快楽を感じるまでには至っていないようだ。
		ENDIF
	ENDIF
	SIF C > 99 && C < 300
		PRINTFORML %CALLNAME:TARGET%はこちらの愛撫に反応して身体を震わせている。
	SIF C > 299 && C < 1000
		PRINTFORML %CALLNAME:TARGET%は軽い電撃でも身体に走ったかのように小さく震えた。
	SIF C > 999 && C < 3000
		PRINTFORML %CALLNAME:TARGET%は明らかに快楽を感じている様子で艶やかな喘ぎ声をあげている。
	SIF C > 2999 && C < 6000
		PRINTFORML %CALLNAME:TARGET%は愛撫に激しく快楽を感じ、自らせがむように手を首にまわしてきた。
	SIF C > 5999
		PRINTFORML %CALLNAME:TARGET%はこちらの愛撫だけで激しく感じまくっており、さらなる刺激を求めてしなだれかかってきた。

;--------------------------------------------------------
;クンニ
;--------------------------------------------------------
ELSEIF SELECTCOM == 1
	A = UP:0

	SIF A < 300
		PRINTFORML %CALLNAME:TARGET%のそこはまだしっとりと閉じたままだ。快楽を感じるにはもう少しかかるかもしれない。
	SIF A > 299 && A < 1000
		PRINTFORML %CALLNAME:TARGET%はクンニの刺激にじっと耐えているものの、確実に感じてきているようだ。
	IF A > 999 && A < 3000
		PRINTFORM 敏感な豆を舐められると感じるのか、%CALLNAME:TARGET%は
		SIF TALENT:35
			PRINT 羞恥に頬を赤く染め手で自分の口を抑えて声が出るのをこらえながら
		SIF TALENT:70 || TALENT:33
			PRINTL 熱い吐息を吐きながら快楽に身をゆだねている。
		SIF TALENT:71 || TALENT:32
			PRINTL 快楽を否定するように首を振って耐えている。
		SIF TALENT:32 == 0 && TALENT:33 == 0 && TALENT:70 == 0 && TALENT:71 == 0
			PRINTL 秘所から愛液を垂らしつつわずかに小さく吐息を漏らした。
	ENDIF
	SIF A > 2999 && A < 6000
		PRINTFORML %CALLNAME:TARGET%の小さな豆を舌で擦り上げてやると%CALLNAME:TARGET%は可愛らしい声を漏らしながらがくがくと身体を揺らした。
	SIF A > 5999 && A < 10000
		PRINTFORML %CALLNAME:TARGET%は明らかな快楽の声をあげ、無意識にこちらの頭を手で押さえてさらにその部分を押し付けてきた。
	IF A > 9999
		IF TALENT:102
			PRINTFORML %CALLNAME:TARGET%は敏感なクリトリスへの責めに激しく身もだえし、あられも無い声を張り上げ続けた。
		ELSE
			PRINTFORML %CALLNAME:TARGET%はクリトリスへの刺激に激しい喘ぎ声を上げながら悶え続けた。
		ENDIF
	ENDIF

;--------------------------------------------------------
;アナル愛撫
;--------------------------------------------------------
ELSEIF SELECTCOM == 2
	A = UP:2

	IF A < 300
		PRINTFORM %CALLNAME:TARGET%は悲鳴を上げて体をよじる。
		IF ABL:5 <= 2
			PRINTL じっくりと開発していけばいいだろう。
		ELSE
			PRINTL 身体の準備が足りなかったようだ。
		ENDIF
	ELSEIF A > 299 && A < 1000
		PRINTFORM %CALLNAME:TARGET%はちいさく身体を揺らした。
		IF ABL:5 <= 2
			PRINTL もうすこし開発してやる必要がありそうだ。
		ELSE
			PRINTL 快楽を得るにはもう少し身体をほぐしてやってからの方が良いようだ。
		ENDIF
	ELSEIF A > 999 && A < 3000
		PRINTFORM %CALLNAME:TARGET%は小さく嬌声を上げて体を跳ねさせた。
		SIF TALENT:106
			PRINT もともとアナルへの刺激に弱かったらしく、
		PRINTL 明らかに快楽を感じ始めている。
	ELSEIF A > 2999 && A < 6000
		PRINTFORML %CALLNAME:TARGET%はアナルへの刺激に明らかな快楽を感じているようだ。
	ELSEIF A > 5999 && A < 10000
		SIF TALENT:106
			PRINT 敏感なアナルを責められると
		SIF TALENT:106 == 0
			PRINT アナルへの刺激に
		PRINTFORM %CALLNAME:TARGET%はよがり声を上げながら
			IF TALENT:70 || TALENT:33
				PRINTL 快楽を訴えた。
			ELSEIF TALENT:71 || TALENT:32
				PRINTL 必死で快楽を否定した。
			ELSE
				PRINTL 悶え狂った。
			ENDIF
	ELSEIF A > 9999
		PRINTFORML %CALLNAME:TARGET%は開発されきったアナルを弄ってやるとたまらないというように悲鳴まじりの喘ぎ声をあげならよがり狂った。
	ENDIF

;--------------------------------------------------------
;自慰
;--------------------------------------------------------
ELSEIF SELECTCOM == 3
	A = UP:0
	B = UP:1
	C = UP:2
	D = UP:17
	E = A+B+C+D

	PRINTFORM %CALLNAME:TARGET%は
	SIF TALENT:35 && ABL:7 <= 3
		PRINT 羞恥に顔を真っ赤に染め、涙を浮かべて
	SIF TALENT:100
	PRINT 小さな身体で
	SIF TALENT:100 == 0 && (TEQUIP:11 || TEQUIP:13)
		PRINT 自らの中に
	SIF TEQUIP:11 && TEQUIP:13
		PRINT ２本のバイブをくわえ込んでいる卑猥な様を見せながら、
	SIF TEQUIP:11
		PRINT 入っているバイブを手で持ち、
	SIF TEQUIP:13
		PRINT 突っ込まれているアナルバイブを持って、
	SIF ABL:7 == 4
		PRINT 露出の快楽に酔っている表情を見せながら
	SIF ABL:7 >= 5
		PRINT 完全に見られる快感を覚えてしまったのか、淫蕩な表情を浮かべて自ら愛液を溢れさせ続ける秘所を押し開いて見せながら
	SIF TEQUIP:53
		PRINT ビデオカメラに向かって
	PRINTL オナニーを続けている。

	IF E < 300
		PRINTL その手の動きはまだ拙く、快楽よりも羞恥の方が強いようだ。
	ELSEIF E >=300 && E <1000
		PRINTL 時折小さく身体を震わせる様は確実に快楽を感じ始めている様子だがまだ没頭しきれないようだ。
	ELSEIF E >= 1000 && E < 3000
		PRINTL その表情には明らかに快楽の兆しが見え、熱っぽい吐息が漏れている。
	ELSEIF E >= 3000 && E < 6000
		PRINTL そして堪えきれない情感に快楽の声を上げながら自慰行為へ没頭していった。
	ELSEIF E >= 6000 && E < 10000
		PRINTL そして自慰行為を続けながら快楽に身体を跳ねさせ、背中でブリッジを作りながら自らの性器を弄り続けた。
	ELSEIF E >= 10000
		PRINTL あまりの快楽になにもかも忘れて蕩けた表情でぐちゅぐちゅと音を立てて性器を弄り続けている。放っとけばいつまでもやり続けそうだ。
	ENDIF

;--------------------------------------------------------
;胸愛撫
;--------------------------------------------------------
ELSEIF SELECTCOM == 5
	A = UP:14

	IF A < 300
		PRINTFORML %CALLNAME:TARGET%はまだ胸で快楽を感じれるまでには至っていないようだ。
	ELSEIF A >= 300 && A < 1000
		PRINTFORML 
	ELSEIF A >= 1000 && A < 3000
		PRINTFORML %CALLNAME:TARGET%の胸を揉み続けてやると乳首は固くしこっていき、熱い吐息を漏らし始めている。
	ELSEIF A >= 3000 && A < 6000
		PRINTFORML %CALLNAME:TARGET%は胸への愛撫にトロンとした表情になってされるがままになっている。
	ELSEIF A >= 6000 && A < 10000
		PRINTFORM %CALLNAME:TARGET%は
		IF TALENT:109
			PRINT その小振りな胸で
		ELSEIF TALENT:140
			PRINT その膨らみのほぼ皆無な胸で
		ENDIF
		PRINT 感じまくっており、胸への刺激だけで法悦を極めようとしている。 
	ELSEIF A >= 10000
		PRINTFORM %CALLNAME:TARGET%の胸を揉み続けてやるとそれだけで感じまくって身体を跳ねさせ、
		SIF TALENT:110
			PRINTL 目の前でその巨乳を弾ませてこちらの目を楽しませてくれた。
		SIF TALENT:141
			PRINTL 目の前でその豊乳を大きく弾ませてこちらの目を楽しませてくれた。
		SIF TALENT:110 == 0
			PRINTL 緩んだ表情で快楽に身を任せていた。
	ENDIF

;--------------------------------------------------------
;ローター
;--------------------------------------------------------
ELSEIF SELECTCOM == 10
	A = UP:0

	IF A < 300
		PRINTFORML %CALLNAME:TARGET%はまだ大きな快楽を得るには至っていないようだ。
	ELSEIF A >= 300 && A < 1000
		PRINTFORML %CALLNAME:TARGET%のクリトリスにローターを押し付けると小さく悲鳴を上げてビクンと身体を跳ねさせた。
	ELSEIF A >= 1000 && A < 3000
		IF TALENT:70
			PRINTFORML %CALLNAME:TARGET%はローターの刺激に気持ちよさそうにその快楽を享受している。
		ELSE
			PRINTFORML %CALLNAME:TARGET%のクリトリスにローターを押し付けると快楽の声を上げながら刺激から逃れようと身をよじった。
		ENDIF
	ELSEIF A >= 3000 && A < 6000
		PRINTFORML %CALLNAME:TARGET%はクリトリスを直接ローターを刺激され激しい快楽に全身を跳ねさせた。
	ELSEIF A >= 6000 && A < 10000
		PRINTFORML %CALLNAME:TARGET%のクリトリスをローターで刺激してやると激しい快楽にのたうち、腰を卑猥にグラインドさせた。
	ELSEIF A >= 10000
		IF TALENT:42
			PRINTFORML %CALLNAME:TARGET%はローターを押し付けられると、強烈すぎる快楽に激しく背中を反らせたまま連続で潮を噴いた。
		ELSE
			PRINTFORML %CALLNAME:TARGET%はローターを押し付けられあまりの快楽に涎すら垂らしながら身体をのた打ち回らせた。
		ENDIF
	ENDIF

;--------------------------------------------------------
;バイブ
;--------------------------------------------------------
ELSEIF SELECTCOM == 11
	A = UP:1

	IF TEQUIP:11
		IF A < 300
			PRINTFORML %CALLNAME:TARGET%はバイブの太さに苦しそうにもがいた。
		ELSEIF A >= 300 && A < 1000
			PRINTFORML %CALLNAME:TARGET%は慣れないバイブの感覚に戸惑いながらも時折突き上げる快楽の衝撃に体を震わせている。
		ELSEIF A >= 1000 && A < 3000
			PRINTFORML %CALLNAME:TARGET%はすでにヴァギナでも快楽を感じれる体になってきたのか、振動するバイブを入れてやると明らかに感じている声を上げた。
		ELSEIF A >= 3000 && A < 6000 
			PRINTFORM %CALLNAME:TARGET%の
			SIF TALENT:100
				PRINT 小さな
			PRINTL ヴァギナにバイブを突っ込んでやるとすぐに快楽に蕩けた表情になった。
		ELSEIF A >= 6000 && A < 10000
			PRINTFORM %CALLNAME:TARGET%は
			SIF TALENT:100
				PRINT その小さな体で
			PRINTL 極太のバイブをくわえ込んで、激しい振動による刺激と快楽にその裸身を若鮎のように跳ねさせている。
		ELSEIF A >= 10000
		PRINTFORML %CALLNAME:TARGET%に振動させたままのバイブで激しく抜き差ししてやると愛液を垂れながして激しい快楽にのたうちまわった。
		ENDIF

	ELSE
		IF A < 300
			PRINTFORML %CALLNAME:TARGET%の体からバイブを引き抜いてやるとほっとした表情をした。
		ELSEIF A >= 300 && A < 1000
			PRINTFORML %CALLNAME:TARGET%はその体からバイブを引き抜く時に小さく可愛らしい喘ぎ声を出した。
		ELSEIF A >= 1000 && A < 3000
				PRINTFORML %CALLNAME:TARGET%の体からバイブを引き抜く時に軽く抜き差ししてやると小さく喘ぎ声を漏らしながら体を悶えさせた。
		ELSEIF A >= 3000 && A < 6000
			PRINTFORML %CALLNAME:TARGET%を散々バイブで責めた後でようやく引き抜いてやると膣から大量の愛液があふれ出た。
		ELSEIF A >= 6000 && A < 10000
			PRINTFORML %CALLNAME:TARGET%はバイブを引き抜いただけで可愛らしい喘ぎ声を上げたので最期にもう一回突っ込んで激しく抜き差ししてやると愛液を噴きながら全身を跳ねさせた。
		ELSEIF A >= 10000
			PRINTFORML %CALLNAME:TARGET%の体からバイブを引き抜く前にバイブを持って思いきり激しく抜き差ししてやると全身をのた打ち回らせてよがり狂った。
		ENDIF

	ENDIF


;--------------------------------------------------------
;Ｅマッサージャ
;--------------------------------------------------------
ELSEIF SELECTCOM == 12
	A = UP:0

	IF A < 300
		PRINTFORML %CALLNAME:TARGET%のクリトリスにマッサージャを押し当ててやったが、振動に耐えるだけで大きな快楽を得るまでにはいたってないようだ。
	ELSEIF A >= 300 && A < 1000
		PRINTFORML %CALLNAME:TARGET%のあそこへマッサージャを押し当ててやるとむずがゆそうにしながらもその表情に快楽の兆しが見える。
	ELSEIF A >= 1000 && A < 3000
		PRINTFORML クリトリスへマッサージャを押し当ててやると%CALLNAME:TARGET%は小さく可愛らしい声をあげながら体を震わせた。
	ELSEIF A >= 3000 && A < 6000
		PRINTFORML %CALLNAME:TARGET%のあそこへマッサージャを押し当ててやると堪えきれない快楽に思わず両足を閉じてマッサージャを締め付けてしまい、%CALLNAME:TARGET%は身悶えた。
	ELSEIF A >= 6000 && A < 10000
		PRINTFORML %CALLNAME:TARGET%はクリトリスに感じるマッサージャの振動がもたらす余りの快楽に必死に逃げようとしながら色っぽい喘ぎ声を出し続けている。
	ELSEIF A >= 10000
		PRINTFORM %CALLNAME:TARGET%はマッサージャを押し当てた瞬間に激しく体をのけぞらせ、
		SIF TALENT:42
		PRINTL あそこから何度も潮をふいた。
		SIF TALENT:42 == 0
		PRINTL 強烈な快楽に身体を跳ねまくらせた。
	ENDIF

;--------------------------------------------------------
;アナルバイブ
;--------------------------------------------------------
ELSEIF SELECTCOM == 13
	A = UP:2

	IF TEQUIP:13
		IF A < 300
			PRINTFORML アナルにバイブを入れてやると%CALLNAME:TARGET%は悲鳴を上げた。まだ苦痛の方が勝るようだ。
		ELSEIF A >= 300 && A < 1000
			PRINTFORML %CALLNAME:TARGET%のアナルは大分受け入れられるようになったようだがまだ少し苦しそうだ。
		ELSEIF A >= 1000 && A < 3000
			PRINTFORML %CALLNAME:TARGET%はアナルにバイブを入れられ少し苦しそうにするも快楽が勝って来たようだ。
		ELSEIF A >= 3000 && A < 6000
			PRINTFORML %CALLNAME:TARGET%はアナルにバイブを突っ込まれて明らかな嬌声を上げて快楽を感じている。
		ELSEIF A >= 6000 && A < 10000
			PRINTFORML %CALLNAME:TARGET%はアナルバイブのもたらす強烈な快楽に表情を緩ませている。
		ELSEIF A >= 10000
			PRINTFORM %CALLNAME:TARGET%はアナルへの刺激に激しく感じまくって
			SIF TALENT:100
			PRINT その小さな身体を
			SIF TALENT:100 == 0
			PRINT その汗まみれの全身を
			PRINTL のたうちまわらせた。
		ENDIF
	ELSE
		IF A < 300
			PRINTFORML %CALLNAME:TARGET%の身体からアナルバイブを引き抜くと安堵のため息を吐いた。
		ELSEIF A >= 300 && A < 1000
			PRINTFORML %CALLNAME:TARGET%のアナルの締め付けを楽しみながらゆっくり引き抜いてやった。
		ELSEIF A >= 1000 && A < 3000
			PRINTFORML アナルからバイブを引き抜く瞬間、%CALLNAME:TARGET%は堪えきれない喘ぎ声を上げた。
		ELSEIF A >= 3000 && A < 6000
			PRINTFORML %CALLNAME:TARGET%の中で振動し続けるバイブを強引に引き抜いてやると甘い快楽の入り混じった悲鳴を上げた。
		ELSEIF A >= 6000 && A < 10000
			PRINTFORML アナルバイブを引き抜く前に軽く抜き差ししてやると%CALLNAME:TARGET%甘い声を上げながらお尻を振った。
		ELSEIF A >= 10000
			PRINTFORML アナルバイブを持って激しく抜き差ししてやると%CALLNAME:TARGET%強烈な快楽に激しく喘ぎ声を上げながら卑猥に腰を振って快楽を貪った。
		ENDIF
	ENDIF

;--------------------------------------------------------
;電動クリキャップ
;--------------------------------------------------------
ELSEIF SELECTCOM == 14
	A = UP:0

	IF A < 300
		PRINTFORML %CALLNAME:TARGET%はクリトリスへの刺激に目を閉じて耐えている。
	ELSEIF A >= 300 && A < 1000
		PRINTFORML クリマッサージャでクリを揉みこんでやると%CALLNAME:TARGET%は小さく喘ぎ声を上げた。
	ELSEIF A >= 1000 && A < 3000
		PRINTFORML %CALLNAME:TARGET%はクリトリスへの刺激に耐え切れず身体を小刻みに震わせて快楽に突き上げられている。
	ELSEIF A >= 3000 && A < 6000
		PRINTFORML %CALLNAME:TARGET%は電動クリキャップの刺激がもたらす激しい快感に腰を突き出すようにして卑猥なダンスを踊っている。
	ELSEIF A >= 6000 && A < 10000
		PRINTFORML %CALLNAME:TARGET%はクリトリスへ送られる快楽に意識を朦朧とさせたまま緩んだ表情を見せている。
	ELSEIF A >= 10000
		PRINTFORML 電動クリキャップを持って動かしてやると%CALLNAME:TARGET%はクリトリスへの凄まじい刺激に耐え切れず愛液を大量に垂れ流しながら身体を跳ねさせた。
	ENDIF

;--------------------------------------------------------
;騎乗位
;--------------------------------------------------------
ELSEIF SELECTCOM == 34
	A = UP:1

	IF A < 299 
	PRINTFORML %CALLNAME:TARGET%は苦しそうにしながら命令に従って腰を振る。だが、まだ快楽を得るには遠いようだ。
	ELSEIF A > 300 && A < 1000
		PRINTFORML %CALLNAME:TARGET%は苦しそうにしながらもその表情には快楽の兆しが見える。
	ELSEIF A > 999 && A < 3000
		PRINTFORML %CALLNAME:TARGET%は腰を振りながらこらえきれない快楽に可愛らしい声を漏らしている。
	ELSEIF A > 2999 && A < 6000
		SIF TALENT:110
		PRINTFORML 目の前で%CALLNAME:TARGET%は大きな乳房を腰の動きに合わせて上下に弾ませながら快楽に浸りきっている。
		SIF TALENT:110 == 0
		PRINTFORML %CALLNAME:TARGET%は大分慣れてきたのか、快楽に突き上げられながら自ら進んで腰を振っている。
	ELSEIF A > 5999 && A < 10000
		PRINTFORM %CALLNAME:TARGET%は
		SIF TALENT:100
		PRINTFORM その小さな身体で
		SIF TALENT:32 || TALENT:71
		PRINTFORML 快楽を否定する言葉を吐きつつ、こらえ切れない喘ぎ声を上げ続けている。
		SIF TALENT:32 == 0 && TALENT:71 == 0
		PRINTFORML 一生懸命腰を振り背中を反らしながら快楽の喘ぎ声を上げた。
	ENDIF
	SIF A > 9999
		PRINTFORML %CALLNAME:TARGET%は腰を振りながら緩みきった、だらしない顔で快楽を貪っている。

;--------------------------------------------------------
;スパンキング、針、鞭
;--------------------------------------------------------
ELSEIF SELECTCOM == 40 || SELECTCOM == 41 || SELECTCOM == 42
	A = UP:9

	IF TFLAG:45 == 0
	SIF A < 300
		B = 0
	SIF A >= 300 && A < 1000
		B = 1
	SIF A >= 1000 && A < 2000
		B = 2
	SIF A >= 2000 && A < 3000
		B = 3
	SIF A >= 3000
		B = 4
	SIF TALENT:40
		B += 1
	SIF B == 0
		PRINTFORML %CALLNAME:TARGET%は悲鳴をこらえながら痛みに耐えた。
	SIF B == 1
		PRINTFORML %CALLNAME:TARGET%は激しい苦痛にかみ締めた歯の隙間から苦しそうなうめき声を上げた。
	SIF B == 2
		PRINTFORML %CALLNAME:TARGET%は苦痛に堪えきれず悲鳴を上げながら全身を跳ねさせた。
	SIF B == 3
		PRINTFORML %CALLNAME:TARGET%は痛みのあまり全身に脂汗を浮かべながら痛苦にのた打ち回った。
	SIF B == 4
		PRINTFORML %CALLNAME:TARGET%はあまりの痛みに意識を飛ばしそうになりながら聞くに堪えない獣のような悲鳴を上げ続けた。
	SIF B >= 5
		PRINTFORML %CALLNAME:TARGET%はあまりの痛みに泣きじゃくりながら必死で許しを請うている。
	ENDIF

;--------------------------------------------------------
;純愛コマンド
;--------------------------------------------------------
;--------------------------------------------------------
;甘い言葉
;--------------------------------------------------------
ELSEIF SELECTCOM == 311
	IF TFLAG:18 <= -1
		;恋人
		IF TALENT:153
			PRINTFORML %CALLNAME:TARGET%は戸惑い、苦笑している。
		ELSE
			PRINTFORML %CALLNAME:TARGET%はあまりの寒さに凍り付いてしまった。
		ENDIF
	ELSEIF TFLAG:18 == 0
		;恋人
		IF TALENT:153
			PRINTFORML %CALLNAME:TARGET%は微笑を浮かべ、%CALLNAME:MASTER%の言葉に聞きいっている。
		ELSE
			PRINTFORML %CALLNAME:TARGET%は少し嬉しそうな表情で、%CALLNAME:MASTER%の言葉を聞いている。
		ENDIF
	ELSEIF TFLAG:18 <= 2
		;恋人
		IF TALENT:153
			PRINTFORML %CALLNAME:TARGET%はうっとりとした表情を浮かべ、%CALLNAME:MASTER%の言葉に聞き惚れている。
		ELSE
			PRINTFORML %CALLNAME:TARGET%は頬を赤く染め、%CALLNAME:MASTER%の目をじっと見つめながら、夢中で話を聞いている。
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%は激しく湧き上がる幸福感に酔いしれている。
	ENDIF
;--------------------------------------------------------
;仲良くなるためのコマンド
;300～339のコマンドで別に地の文を表示したい場合はこの分岐の前か、この分岐の条件文からそのコマンドは除く必要あり
;--------------------------------------------------------
ELSEIF SELECTCOM >= 300 && SELECTCOM <= 339 && (SELECTCOM != 323 || TEQUIP:2) && SELECTCOM != 310
	A = UP:15

	;現在値が1億を越えたら、もう全部大量入手扱いにする
	IF PALAM:15 > 100000000
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に完全に愛情の篭った眼差しを向けている。
	ELSEIF A < 300
		IF TALENT:11 || TALENT:12 || TALENT:15 || TALENT:16 || TALENT:32 || TALENT:34 || TALENT:20 || TALENT:21 || TALENT:82 && TALENT:PLAYER:122
			PRINTFORML %CALLNAME:TARGET%はこちらの様子を警戒している。
		ELSE
			PRINTFORML %CALLNAME:TARGET%はまだまだぎこちない様子だ。
		ENDIF
	ELSEIF A < 1000
		IF TALENT:11 || TALENT:12 || TALENT:15 || TALENT:16 || TALENT:32 || TALENT:34 || TALENT:20 || TALENT:21 || TALENT:82 && TALENT:PLAYER:122
			PRINTFORML 少しは打ち解けてきたようだが、%CALLNAME:TARGET%はまだこちらに気を許すつもりは無いようだ。
		ELSE
			PRINTFORML %CALLNAME:TARGET%とも少しは打ち解けてきたようだ。
		ENDIF
	ELSEIF A < 3000
		IF TALENT:11 || TALENT:12 || TALENT:15 || TALENT:16 || TALENT:32 || TALENT:34 || TALENT:20 || TALENT:21 || TALENT:82 && TALENT:PLAYER:122
			PRINTFORML %CALLNAME:TARGET%はこちらの事を信用する気になったようだ。
		ELSE
			PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%と一緒に居る事を楽しんでいるようだ。
		ENDIF
	ELSEIF A < 10000
		PRINTFORML %CALLNAME:TARGET%は少しずつ%CALLNAME:PLAYER%に好意を寄せてきたようだ。
	ELSEIF A < 30000
		PRINTFORML %CALLNAME:TARGET%はかなり%CALLNAME:PLAYER%の事を気に入っているようだ。
	ELSEIF A < 1000000
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%とずっとこうしていたいと思っているようだ。
	ELSE
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に完全に愛情の篭った眼差しを向けている。
	ENDIF

;--------------------------------------------------------
;キス系コマンド
;--------------------------------------------------------
ELSEIF SELECTCOM >= 340 && SELECTCOM <= 359
	A = UP:11

	PRINTFORM %CALLNAME:TARGET%は

	;反感で追加メッセージ	反感がカンストは無いだろうから、無視
	IF A == 0
		PRINTFORML キスに抵抗する様子を全く見せなかった。
	ELSEIF A < 100
		PRINTFORML 僅かに抵抗する素振りを見せながらも、キスを素直に受け入れた。
	ELSEIF A < 500
		PRINTFORML 少し抵抗したが、結局はキスを受け入れた。
	ELSEIF A < 1500
		PRINTFORML 恨みがましい視線をこちらに向けながらキスをした。
	ELSE
		PRINTFORML かなり嫌悪感を抱いているようだ。
	ENDIF

;--------------------------------------------------------
;胸愛撫
;--------------------------------------------------------
ELSEIF SELECTCOM == 360
	A = UP:14

	IF A > 100000000
		PRINTFORM %CALLNAME:TARGET%の胸を揉み続けてやると、それだけで身体を跳ねさせ、
		SIF TALENT:110
		PRINTL 目の前でその巨乳を弾ませてこちらの目を楽しませてくれた。
		SIF TALENT:110 == 0
		PRINTL 緩んだ表情で快楽に身を任せていた。
	ELSEIF A < 50
		PRINTFORML %CALLNAME:TARGET%にはまだくすぐったいという感情の方が強いようだ。
	ELSEIF A >= 50 && A < 200
		PRINTFORML %CALLNAME:TARGET%は小さな声を漏らし、頬が少しずつ赤く染まりだした。
	ELSEIF A >= 200 && A < 1000
		PRINTFORML %CALLNAME:TARGET%は熱い吐息を漏らしながら、されるがままになっている。
	ELSEIF A >= 1000 && A < 3000
		PRINTFORML %CALLNAME:TARGET%は胸への愛撫にトロンとした表情になっている。
	ELSEIF A >= 3000 && A < 10000
		PRINTFORM %CALLNAME:TARGET%は
		SIF TALENT:109
			PRINT その小振りな胸で
		PRINT 強い快感を感じ、服越しの刺激だけでも法悦を極めようとしている。 
	ELSEIF A >= 10000
		PRINTFORM %CALLNAME:TARGET%の胸を揉み続けてやると、それだけで身体を跳ねさせ、
		SIF TALENT:110
			PRINTL 目の前でその巨乳を弾ませてこちらの目を楽しませてくれた。
		SIF TALENT:110 == 0
			PRINTL 緩んだ表情で快楽に身を任せていた。
	ENDIF

;--------------------------------------------------------
;愛撫
;--------------------------------------------------------
ELSEIF SELECTCOM == 361
	A = UP:0

	IF A > 100000000
		PRINTFORML %CALLNAME:TARGET%はこちらの愛撫だけで激しく感じ、さらなる刺激を求めてしなだれかかってきた。
	ELSEIF A < 50
		PRINTFORML %CALLNAME:TARGET%にはまだくすぐったいという感情の方が強いようだ。
	ELSEIF A >= 50 && A < 200
		PRINTFORML %CALLNAME:TARGET%は小さな声を漏らし、頬が少しずつ赤く染まりだした。
	ELSEIF A >= 200 && A < 1000
		PRINTFORML %CALLNAME:TARGET%は熱い吐息を漏らしながら、されるがままになっている。
	ELSEIF A >= 1000 && A < 3000
		PRINTFORML %CALLNAME:TARGET%は軽い電撃でも身体に走ったかのように小さく震えた。
	ELSEIF A >= 3000 && A < 10000
		PRINTFORML %CALLNAME:TARGET%は明らかに快楽を感じている様子で、下着越しの刺激だけでも艶やかな喘ぎ声をあげている。 
	ELSEIF A >= 10000
		PRINTFORML %CALLNAME:TARGET%はこちらの愛撫だけで激しく感じ、さらなる刺激を求めてしなだれかかってきた。
	ENDIF

;--------------------------------------------------------
;着衣ローター
;--------------------------------------------------------
ELSEIF SELECTCOM == 367
	A = UP:0

	;絶頂
	IF TFLAG:29 > 0
		IF A < 10000
			PRINTFORML %CALLNAME:TARGET%はローターの刺激に熱い吐息を漏らしながら小さく震えている。
		ELSEIF A < 100000
			PRINTFORM %CALLNAME:TARGET%は首筋まで赤く染め
			IF TALENT:35
				PRINTFORML 声が出るのをこらえようと、%CALLNAME:PLAYER%に頭を押し付けてきた。
			ELSEIF TALENT:70 || TALENT:33
				PRINTL 緩んだ表情で小さく喘ぎ声をあげている。
			ELSEIF TALENT:71 || TALENT:32
				PRINTL 快楽を否定するように首を振りながらもこらえ切れない喘ぎ声が口から漏れている。
			ELSE
				PRINTL 腰を卑猥に震わせている。
			ENDIF
		ELSE
			IF TALENT:102
				PRINTFORML %CALLNAME:TARGET%は敏感なクリトリスへの刺激に身悶えし、%CALLNAME:PLAYER%にしなだれかかってきた。
			ELSE
				PRINTFORML %CALLNAME:TARGET%はクリトリスへの刺激に耐えきれずしなだれかかってきた。
			ENDIF
		ENDIF
	;デート中かつ絶頂せず
	ELSEIF TEQUIP:2
		;戦闘訓練
		IF TFLAG:17 == 1
			PRINTFORM %CALLNAME:TARGET%はいつの間にか集まった観客の視線
			IF TALENT:35
				PRINTFORML を気にして恥ずかしそうに俯いている…
			ELSEIF TALENT:28
				PRINTFORML に熱い吐息を漏らしている…
			ELSE
				PRINTFORML を気にしてわずかに頬を染めている…
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:TARGET%は
			IF TALENT:35
				PRINTFORML 周囲を気にして羞恥に頬を朱に染めている…
			ELSEIF TALENT:28
				PRINTFORML 頬を上気させ熱い吐息を漏らしている…
			ELSE
				PRINTFORML 周囲を気にして薄っすらと頬が上気している…
			ENDIF
		ENDIF
	ELSE
		IF A < 300
			PRINTFORML %CALLNAME:TARGET%が快楽を感じるにはもう少しかかるかもしれない。
		ELSEIF A < 1000
			PRINTFORML %CALLNAME:TARGET%はローターの刺激に時折むずがゆそうにしながらも、確実に感じてきているようだ。
		ELSEIF A < 3000
			PRINTFORML %CALLNAME:TARGET%は頬が少しずつ赤く染まりだしたものの、まだくすぐったいという感情の方が強いようだ。
		ELSEIF A < 6000
			PRINTFORM %CALLNAME:TARGET%は羞恥に頬を赤く染め
			SIF TALENT:35
				PRINT 手で自分の口をおさえて声が出るのをこらえながら
			IF TALENT:70 || TALENT:33
				PRINTL 熱い吐息を吐きながら表情を緩ませている。
			ELSEIF TALENT:71 || TALENT:32
				PRINTL 快楽を否定するように首を振って耐えている。
			ELSE
				PRINTL わずかに小さく吐息を漏らした。
			ENDIF
		ELSEIF A < 10000
			PRINTFORML %CALLNAME:TARGET%はローターの刺激に熱い吐息を漏らしながら小さく震えている。
		ELSE
			IF TALENT:102
				PRINTFORML %CALLNAME:TARGET%は敏感なクリトリスへの刺激に身悶えし、%CALLNAME:PLAYER%にしなだれかかってきた。
			ELSE
				PRINTFORML %CALLNAME:TARGET%はクリトリスへの刺激に耐えきれずしなだれかかってきた。
			ENDIF
		ENDIF
	ENDIF
;--------------------------------------------------------
;独自口上用
;--------------------------------------------------------
ELSEIF SELECTCOM == 280
	CALL TEXT280
ELSEIF SELECTCOM == 281
	CALL TEXT281
ELSEIF SELECTCOM == 282
	CALL TEXT282
ELSEIF SELECTCOM == 283
	CALL TEXT283
ELSEIF SELECTCOM == 284
	CALL TEXT284
ELSEIF SELECTCOM == 410
	CALL TEXT410
ELSEIF SELECTCOM == 411
	CALL TEXT411
ELSEIF SELECTCOM == 412
	CALL TEXT412
ELSEIF SELECTCOM == 413
	CALL TEXT414
ELSEIF SELECTCOM == 414
	CALL TEXT414
ENDIF
RETURN 0

@SAMPLE
;ELSEIF SELECTCOM == 10
A = UP:0

IF A < 300
	PRINTFORML 
ELSEIF A >= 300 && A < 1000
	PRINTFORML 
ELSEIF A >= 1000 && A < 3000
	PRINTFORML 
ELSEIF A >= 3000 && A < 6000
	PRINTFORML 
ELSEIF A >= 6000 && A < 10000
	PRINTFORML 
ELSEIF A >= 10000
	PRINTFORML 
ENDIF
