@COM_ABLE203
;クスコされる実行判定
;COM_ABLE共通判定
CALL COM_ABLE_COMMON, SELECTCOM
SIF RESULT == 0
	RETURN 0
;ウフフ中じゃないと無理
SIF TFLAG:44 == 0
	RETURN 0
;解除はいつでも可能
;SIF TEQUIP:61
;	RETURN 1
;マスターがオトコだとダメ
SIF TALENT:MASTER:122
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:40 == 0 && NOITEM == 0
	RETURN 0
;マスターが処女だとダメ
SIF TALENT:MASTER:0 == 1
	RETURN 0
;アイマスク中だと意味が無いのでだめ
SIF TEQUIP:43
	RETURN 0
;縄縛り中も意味が無いのでだめ
SIF TEQUIP:44
	RETURN 0
;ギャグボール中も意味がないのでだめ
SIF TEQUIP:45
	RETURN 0
;クスコ中はどうしましょうね
;SIF TEQUIP:61
;	RETURN 0
;従順4以下・レズっ気4以下のTARGETの場合クスコしてくれない（サドならＯＫ）
SIF (ABL:0 <= 4 || ABL:9 <= 4) && TALENT:83 == 0
	RETURN 0
;技巧が5以下だとできない
SIF ABL:2 < 5
	RETURN 0
RETURN 1

@COM203
;クスコされる

PRINTL クスコされる

;-------------------------------------------------
;実行できるかの判定
;-------------------------------------------------
CALL COM_ORDER_COMMON
IF RESULT == 0
	;目標に届かない場合は白い目で見られる
	PRINTFORML %CALLNAME:TARGET%は信じられないものを見るような目でこちらを見ている……
	PRINTFORMW %CALLNAME:TARGET%の好感度が50下がりました。
	CFLAG:2 -= 50
	RETURN 0
ENDIF

;-------------------------------------------------
;実行決定
;-------------------------------------------------
STR:0 = クスコされる
;EVENT_Mから呼び出す
CALL TRAIN_MESSAGE_B

;-------------------------------------------------
;射精ゲージチェック
;-------------------------------------------------
;入れようかどうか迷ったけど削除

;-------------------------------------------------
;ソースの計算
;-------------------------------------------------
LOSEBASE:0 += 50
LOSEBASE:1 += 150

SOURCE:8 = 1000
SOURCE:13 = 500
SOURCE:14 = 500
;3:情愛　5:達成感　4:性行動　7:中毒充足　8:不潔
;11:欲情追加　13:屈従　14:逸脱　20:征服

;ABL:欲情をみる
SOURCE:11 = 500
;ふたなり、ふたなり淫核、排卵誘発状態ならそれぞれ追加
SIF TALENT:121
	SOURCE:11 += 500
SIF TALENT:75 && TALENT:121
	SOURCE:11 += 500
SIF TEQUIP:MASTER:86
	SOURCE:11 += 500
IF ABL:1 == 0
	TIMES SOURCE:11 , 0.25
ELSEIF ABL:1 == 1
	TIMES SOURCE:11 , 0.50
ELSEIF ABL:1 == 2
	TIMES SOURCE:11 , 0.75
ELSEIF ABL:1 == 3
	TIMES SOURCE:11 , 1.00
ELSEIF ABL:1 == 4
	TIMES SOURCE:11 , 1.25
ELSEIF ABL:1 == 5
	TIMES SOURCE:11 , 1.50
ELSE
	;ここは放尿から
	SOURCE:11 += SOURCE:11 + (ABL:11 * 350)
ENDIF

;ABL:奉仕精神をみる
;コンドーム精飲からコピペして改変
;倒錯的なら情愛追加
SIF TALENT:80
	SOURCE:3 += 500
IF ABL:6 == 0
	SOURCE:4 = 420
	SOURCE:5 = 150
	TIMES SOURCE:8 , 4.00
	TIMES SOURCE:3 , 1.00
ELSEIF ABL:6 == 1
	SOURCE:4 = 500
	SOURCE:5 = 300
	TIMES SOURCE:8 , 2.50
	TIMES SOURCE:3 , 1.20
ELSEIF ABL:6 == 2
	SOURCE:4 = 580
	SOURCE:5 = 600
	TIMES SOURCE:8 , 1.50
	TIMES SOURCE:3 , 1.40
ELSEIF ABL:6 == 3
	SOURCE:4 = 660
	SOURCE:5 = 900
	TIMES SOURCE:8 , 1.00
	TIMES SOURCE:3 , 1.60
ELSEIF ABL:6 == 4
	SOURCE:4 = 740
	SOURCE:5 = 1500
	TIMES SOURCE:8 , 0.50
	TIMES SOURCE:3 , 1.80
ELSE
	SOURCE:4 = 820
	SOURCE:5 = 2200
	TIMES SOURCE:8 , 0.10
	TIMES SOURCE:3 , 2.00
ENDIF

;ABL:サドっ気をみる
A = 0
;淫乱なら中毒充足に梃入れ
SIF TALENT:76
	SOURCE:7 += 500
IF ABL:15 == 0
	A += 400
	SOURCE:7 += 100
	SOURCE:20 = 300
	;倒錯的なら情愛も追加
	SIF TALENT:80
		TIMES SOURCE:3 , 1.00
ELSEIF ABL:15 == 1
	A += 500
	SOURCE:7 += 500
	SOURCE:20 = 600
	SIF TALENT:80
		TIMES SOURCE:3 , 1.20
ELSEIF ABL:15 == 2
	A += 700
	SOURCE:7 += 900
	SOURCE:20 = 900
	SIF TALENT:80
		TIMES SOURCE:3 , 1.40
ELSEIF ABL:15 == 3
	A += 900
	SOURCE:7 += 1300
	SOURCE:20 = 1200
	SIF TALENT:80
		TIMES SOURCE:3 , 1.60
ELSEIF ABL:15 == 4
	A += 1100
	SOURCE:7 += 1700
	SOURCE:20 = 1500
	SIF TALENT:80
		TIMES SOURCE:3 , 1.80
ELSE
	;数値を使いまわすためAに代入、主導権相手時にほぼ100%分入るように調整
	A += 500 + (ABL:15 * 350) / 2
	SOURCE:7 += 500 + (ABL:15 * 500)
	SOURCE:20 += 600 + (ABL:15 * 400)
	SIF SOURCE:3 > 0
		SOURCE:3 += SOURCE:3 + ((ABL:6 + ABL:15) * 100)
ENDIF
SOURCE:20 += A
SIF TALENT:220 && ASSIPLAY == 0
	TIMES SOURCE:7 , 1.20

;ABL:技巧をみる
IF ABL:2 == 0
	TIMES SOURCE:4 , 0.50
	TIMES SOURCE:5 , 0.50
ELSEIF ABL:2 == 1
	TIMES SOURCE:4 , 0.80
	TIMES SOURCE:5 , 0.80
ELSEIF ABL:2 == 2
	TIMES SOURCE:4 , 1.00
	TIMES SOURCE:5 , 1.00
ELSEIF ABL:2 == 3
	TIMES SOURCE:4 , 1.20
	TIMES SOURCE:5 , 1.20
ELSEIF ABL:2 == 4
	TIMES SOURCE:4 , 1.50
	TIMES SOURCE:5 , 1.50
ELSE
	TIMES SOURCE:4 , 2.00
	TIMES SOURCE:5 , 2.00
ENDIF

;主導権をチェック
IF TFLAG:45 == 0
	;主導権が主人公側
	;ABL:従順をみる
	IF ABL:0 <= 1
		SOURCE:16 += 300 + (ABL:0 * 50)
	ELSEIF ABL:0 <= 3
		SOURCE:16 += 600 + (ABL:0 * 100)
	ELSEIF ABL:0 <= 5
		SOURCE:16 += 800 + (ABL:0 * 120)
	ELSEIF ABL:0 <= 8
		SOURCE:16 += 1000 + (ABL:0 * 140)
	ELSEIF ABL:0 <= 11
		SOURCE:16 += 1200 + (ABL:0 * 160)
	ELSE
		SOURCE:16 += 1500 + (ABL:0 * 180)
	ENDIF
ELSE
	;主導権が相手側
	;ABL:サドっ気を使いまわす
	SOURCE:20 *= 2
ENDIF

;好感度ランダム変化
A = RAND:10 + 5
SIF TALENT:85 || TALENT:153
	A += 2
SIF TALENT:88
	A += 3
;合意がないと好感度が下がる
IF TALENT:152 == 0 && TFLAG:48 == 0
	A -= 8
	SOURCE:15 += 300
ENDIF
TFLAG:30 += A
TFLAG:37 += A / 2

;女同士ならレズ経験が増える
IF TALENT:122 == 0 && TALENT:PLAYER:122 == 0
	PRINTFORML %EXPNAME:40%＋4
	EXP:40 += 4
ENDIF

TFLAG:100 = 1

;既成事実１に相当
TFLAG:200 = 2

;調教者がふたなり
IF TALENT:PLAYER:121
	SOURCE:13 /= 2
ENDIF

RETURN 1

@TRAIN_MESSAGE_COM203
;クスコの着脱どうしよう
SIF TEQUIP:53
	PRINT ビデオカメラの前で
SIF TEQUIP:57
	PRINT 鏡に映りこむように、
IF TFLAG:45
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:MASTER%に
ELSE
	PRINTFORM %CALLNAME:MASTER%は%CALLNAME:TARGET%に
ENDIF
;暫定的にムードを使用
IF TFLAG:37 > 30 || TALENT:220
	PRINT 淫らにヒクつく膣
ELSEIF TFLAG:50 > 10
	PRINT 濡れそぼった膣
ELSEIF TALENT:100 && TALENT:109
	PRINT 幼いワレメ
ELSEIF TALENT:100
	PRINT 小さな膣
ELSE
	PRINT 陰唇
ENDIF
PRINT の奥まで膣鏡を挿入
IF TFLAG:45
	PRINT し、割り開いた
ELSE
	PRINT させ、割り開いた
ENDIF
PRINTL 　
PRINTFORM %CALLNAME:TARGET%は
IF CFLAG:PLAYER:103 > 0
	SIF CFLAG:PLAYER:103 >= 10
		PRINT たっぷりと
	PRINT 子種
	IF RAND:2
		PRINT で満たされている様を
	ELSE
		PRINT が溜まっている様を
	ENDIF
;暫定的にムード値or排卵誘発剤
ELSEIF TFLAG:37 > 30 || TEQUIP:MASTER:86
	IF TEQUIP:MASTER:86 || TALENT:220
		PRINT 広げられた秘肉から大量の白濁した愛液が流れている様を
	ELSEIF TALENT:37 > 30
		PRINT 子宮口が降りてきている様を
	ELSE
		PRINT ぷっくりと膨れたＧスポットの様子を
	ENDIF
ELSE
	IF RAND:2
		PRINT 膣壁が妖しく蠢いている様を
	ELSE
		PRINT 洪水のように愛液を垂れ流している様を
	ENDIF
ENDIF
PRINT 口に出して
;さどっけが高い
IF ABL:15 >= 5
	PRINT 冷やかし
ELSE
	PRINTFORM %CALLNAME:MASTER%に伝え
ENDIF
;倒錯的
IF TALENT:80
	PRINT 、その中
	;サドで女でふたなりでない
	IF TALENT:83 && TALENT:MASTER:122 == 0 && TALENT:121 == 0
		PRINT に自らの唾液を垂らし
	;サドで男かふたなり
	;ELSEIF TALENT:83 && (TALENT:121 || TALENT:122)
	;	PRINT へ向けて自分の精を放っ
	;サドのみ
	ELSE
		PRINT を指でつつい
	ENDIF
	PRINT てかき混ぜ
ENDIF
PRINT ている

IF TALENT:88 || TALENT:220
	PRINTFORM %CALLNAME:MASTER%は愛する
	IF TALENT:220
		PRINT 御主人様
	ELSE
		PRINT 人
	ENDIF
	IF TALENT:80 && TALENT:MASTER:122 == 0 && TALENT:121 == 0
		PRINT に体の奥まで
		IF ABL:15 >= 5
			PRINT 犯される
		ELSE
			PRINT 見せつける
		ENDIF
		PRINT 快楽に溺れ
		SIF TALENT:72
			PRINTFORM 、涎を垂らし
		SIF TALENT:70 || TALENT:80 || TALENT:79
			PRINT ながら自ら腰を突き上げ
		PRINTL ている…
	ELSEIF TEQUIP:MASTER:86
		PRINTFORML の見ている前で、%CALLNAME:MASTER%は白濁した蜜をとろとろと床へこぼした…
	ELSE
		PRINTL に割り開かれた場所を見られ、興奮のあまり涎と涙と愛液を溢れさせている…
	ENDIF
ELSEIF TALENT:85 && TALENT:153
	PRINTFORML %CALLNAME:MASTER%は秘洞の奥まで見られていることに倒錯的な興奮を覚えている…
ENDIF

;-------------------------------------------------
;固有の実行判定
;-------------------------------------------------
@COM_ORDER_203
A = 0
S = 0
V = 150

;すべての命令に共通の要素を考慮
;(従順が高いと命令に従いやすいなど)
CALL COM_ORDER

;ABL:欲望
IF ABL:1
	A += ABL:1 * 3
ENDIF
;ABL:奉仕精神
IF ABL:6
	A += ABL:6 * 3
ENDIF
;ABL:サドっ気
IF ABL:15
	A += ABL:15 * 4
ENDIF
;快楽刻印
IF MARK:1
	A += MARK:1 * 3
ENDIF

;PALAM:欲情
IF PALAM:5 < PALAMLV:1
	L = 0
ELSEIF PALAM:5 < PALAMLV:2
	L = 1
ELSEIF PALAM:5 < PALAMLV:3
	L = 2
ELSEIF PALAM:5 < PALAMLV:4
	L = 3
ELSEIF PALAM:5 < PALAMLV:5
	L = 4
ELSE
	L = 5
ENDIF
IF L
	A += L * 3
ENDIF

;臆病
IF TALENT:10
	A -= 10
ENDIF
;一線越えない（合意が無い場合）
IF TALENT:27 && TALENT:152 == 0
	A -= 20
ENDIF
;貞操観念
IF TALENT:30
	A -= 5
ENDIF
;貞操無頓着
IF TALENT:31
	A += 5
ENDIF
;恥じらい
IF TALENT:35
	A -= 5
ENDIF
;汚臭鈍感
IF TALENT:61
	A += 5
ENDIF
;汚臭敏感
IF TALENT:62
	A -= 5
ENDIF
;献身的
IF TALENT:63
	A += 5
ENDIF
;汚れ無視
IF TALENT:64
	A += 3
ENDIF
;快感に素直
IF TALENT:70
	A += 5
ENDIF
;快感の否定
IF TALENT:71
	A -= 5
ENDIF
;淫乱
IF TALENT:76
	A += 10
ENDIF
;恋慕
IF TALENT:85 && ASSIPLAY == 0
	A += 5
ENDIF
;倒錯的
IF TALENT:80
	A += 7
ENDIF
;サド
IF TALENT:83
	A += 5
ENDIF
;小悪魔
IF TALENT:87
	A += 3
ENDIF
;禁断の知識
IF TALENT:125
	A += 3
ENDIF
;主人
IF TALENT:220
	A += 3
ENDIF

;好感度補正-
IF CFLAG:2 < 3000
	B = 0
	IF CFLAG:2 < 15000
		B = 50
	ELSEIF CFLAG:2 < 2000
		B = 20
	ELSEIF CFLAG:2 < 2500
		B = 10
	ELSE
		B = 5
	ENDIF
	A -= B
ENDIF

;好感度補正+
IF CFLAG:2 >= 4000
	B = 0
	IF CFLAG:2 < 5000
		B = 5
	ELSE
		B = CFLAG:2 / 500
	ENDIF
	A += B
ENDIF

;合意
IF TALENT:152 == 0
	A -= 10
ENDIF

IF ((FLAG:23 & 1p27) == 0) && FLAG:2 != 1
	;ABL:欲望
	IF ABL:1
		SIF S
			PRINT  + 
		PRINTS ABLNAME:1
		PRINTV 'LV,ABL:1,'(,ABL:1 * 3,')
		S = 1
	ENDIF
	;ABL:奉仕精神
	IF ABL:6
		SIF S
			PRINT  + 
		PRINTS ABLNAME:6
		PRINTV 'LV,ABL:6,'(,ABL:6 * 3,')
		S = 1
	ENDIF
	;ABL:サドっ気
	IF ABL:15
		SIF S
			PRINT  + 
		PRINTS ABLNAME:15
		PRINTV 'LV,ABL:15,'(,ABL:15 * 4,')
		S = 1
	ENDIF

	;快楽刻印
	IF MARK:1
		SIF S
			PRINT  + 
		PRINTS MARKNAME:1
		PRINTV 'LV,MARK:1,'(,MARK:1 * 3,')
		S = 1
	ENDIF

	;PALAM:欲情
	IF PALAM:5 < PALAMLV:1
		L = 0
	ELSEIF PALAM:5 < PALAMLV:2
		L = 1
	ELSEIF PALAM:5 < PALAMLV:3
		L = 2
	ELSEIF PALAM:5 < PALAMLV:4
		L = 3
	ELSEIF PALAM:5 < PALAMLV:5
		L = 4
	ELSE
		L = 5
	ENDIF
	IF L
		SIF S
			PRINT  + 
		PRINTS PALAMNAME:5
		PRINTV 'LV,L,'(,L * 3,')
		S = 1
	ENDIF

	;臆病
	IF TALENT:10
		PRINT  - 
		PRINTS TALENTNAME:10
		PRINTV '(,10,')
		S = 1
	ENDIF
	;一線越えない(合意が無い場合)
	IF TALENT:27 && TALENT:152 == 0
		PRINT  - 
		PRINTS TALENTNAME:27
		PRINTV '(,20,')
		S = 1
	ENDIF
	;貞操観念
	IF TALENT:30
		PRINT  - 
		PRINTS TALENTNAME:30
		PRINTV '(,5,')
		S = 1
	ENDIF
	;貞操無頓着
	IF TALENT:31
		PRINT  + 
		PRINTS TALENTNAME:71
		PRINTV '(,5,')
		S = 1
	ENDIF
	;恥じらい
	IF TALENT:35
		PRINT  - 
		PRINTS TALENTNAME:35
		PRINTV '(,5,')
		S = 1
	ENDIF
	;汚臭鈍感
	IF TALENT:61
		PRINT  + 
		PRINTS TALENTNAME:61
		PRINTV '(,5,')
		S = 1
	ENDIF
	;汚臭敏感
	IF TALENT:62
		PRINT  - 
		PRINTS TALENTNAME:62
		PRINTV '(,5,')
		S = 1
	ENDIF
	;献身的
	IF TALENT:63
		PRINT  + 
		PRINTS TALENTNAME:63
		PRINTV '(,5,')
		S = 1
	ENDIF
	;快感の否定
	IF TALENT:64
		PRINT  + 
		PRINTS TALENTNAME:64
		PRINTV '(,3,')
		S = 1
	ENDIF
	;快感に素直
	IF TALENT:10
		PRINT  - 
		PRINTS TALENTNAME:70
		PRINTV '(,5,')
		S = 1
	ENDIF
	;快感の否定
	IF TALENT:71
		PRINT  - 
		PRINTS TALENTNAME:71
		PRINTV '(,5,')
		S = 1
	ENDIF
	;淫乱
	IF TALENT:76
		PRINT  + 
		PRINTS TALENTNAME:76
		PRINTV '(,10,')
		S = 1
	ENDIF
	;恋慕
	IF TALENT:71 && ASSIPLAY == 0
		PRINT  + 
		PRINTS TALENTNAME:71
		PRINTV '(,5,')
		S = 1
	ENDIF
	;倒錯的
	IF TALENT:80
		PRINT  - 
		PRINTS TALENTNAME:80
		PRINTV '(,7,')
		S = 1
	ENDIF
	;サド
	IF TALENT:83
		PRINT  + 
		PRINTS TALENTNAME:83
		PRINTV '(,5,')
		S = 1
	ENDIF
	;小悪魔
	IF TALENT:87
		PRINT  + 
		PRINTS TALENTNAME:87
		PRINTV '(,5,')
		S = 1
	ENDIF
	;禁断の知識
	IF TALENT:125
		PRINT  + 
		PRINTS TALENTNAME:125
		PRINTV '(,3,')
		S = 1
	ENDIF
	;主人
	IF TALENT:220
		PRINT  + 
		PRINTS TALENTNAME:220
		PRINTV '(,3,')
		S = 1
	ENDIF

	;好感度補正-
	IF CFLAG:2 < 3000
		B = 0
		IF CFLAG:2 < 15000
			B = 50
		ELSEIF CFLAG:2 < 2000
			B = 20
		ELSEIF CFLAG:2 < 2500
			B = 10
		ELSE
			B = 5
		ENDIF
		PRINT 好感度不足
		PRINTV '(,B,')
		S = 1
	ENDIF

	;好感度補正+
	IF CFLAG:2 >= 4000
		B = 0
		IF CFLAG:2 < 5000
			B = 5
		ELSE
			B = CFLAG:2 / 500
		ENDIF
		PRINT 好感度
		PRINTV '(,B,')
		S = 1
	ENDIF

	;合意
	IF TALENT:152 == 0
		PRINT  - 
		PRINTFORM %TALENTNAME:152%無し
		PRINTV '(,10,')
		S = 1
	ENDIF

	;合計を表示(150以上で実行)
	PRINT  = 
	PRINTV A


	SIF A < V
		PRINT  < 
	SIF A == V
		PRINT  = 
	SIF A > V
		PRINT  > 
	PRINT 実行値
	PRINTV V
	WAIT
ENDIF

