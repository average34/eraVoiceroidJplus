@COM_ABLE192
;パイズリする実行判定
;COM_ABLE共通判定
CALL COM_ABLE_COMMON, SELECTCOM
SIF RESULT == 0
	RETURN 0
;ウフフ中じゃないと無理
SIF TFLAG:44 == 0
	RETURN 0
;性器が汚れているとMASTERの場合パイズリするはできない（調教者が汚れ無視か愛液、Ｐ、母乳のよごれならＯＫ）
SIF (STAIN:2 >= 4) && ((STAIN:2 != 18) && (STAIN:2 != 19)) && (ASSIPLAY == 0) && (TALENT:MASTER:64 == 0)
	RETURN 0
;性器が汚れており、汚臭敏感で従順3以下のASSIの場合パイズリはできない（調教者が汚れ無視か愛液、Ｐ、母乳のよごれならＯＫ）
IF (STAIN:2 >= 4) && ((STAIN:2 != 18) && (STAIN:2 != 19)) && ASSIPLAY
	SIF ABL:ASSI:0 <= 3 && TALENT:ASSI:62 && TALENT:ASSI:64 == 0
		RETURN 0
ENDIF
;[絶壁]か[オトコ]だと不可
SIF TALENT:PLAYER:140 || TALENT:PLAYER:122
	RETURN 0
;[爆乳]なら技巧LV1以上、[巨乳]でなければ技巧3以上、[貧乳]だと技巧4以上必要
SIF ((TALENT:PLAYER:141 == 1) && (ABL:2 == 0)) || ((TALENT:PLAYER:141 == 0) && (TALENT:PLAYER:110 == 1) && (ABL:2 < 2)) || ((TALENT:PLAYER:141 == 0) && (TALENT:PLAYER:110 == 0) && (ABL:2 < 3)) || (TALENT:PLAYER:109 && (ABL:2 < 4))
	RETURN 0
;オトコかふたなりじゃないとダメ
SIF (TALENT:121 || TALENT:122) == 0
	RETURN 0
;オナホール使用中はダメ
SIF TEQUIP:17
	RETURN 0
;触手調教中はダメ
SIF TEQUIP:90
	RETURN 0
RETURN 1

@COM192
;パイズリする


PRINTL パイズリする
STR:0 = パイズリする
CALL TRAIN_MESSAGE_B

LOSEBASE:0 += 50
LOSEBASE:1 += 180

SOURCE:3 = 1000
SOURCE:5 = 1000
SOURCE:12 = 220
SOURCE:14 = 50

;ABL:Ｃ感覚をみる
IF ABL:3 == 0
	SOURCE:0 = 1000
	SOURCE:7 = 800
ELSEIF ABL:3 == 1
	SOURCE:0 = 1600
	SOURCE:7 = 1000
ELSEIF ABL:3 == 2
	SOURCE:0 = 2200
	SOURCE:7 = 1300
ELSEIF ABL:3 == 3
	SOURCE:0 = 2700
	SOURCE:7 = 1700
ELSEIF ABL:3 == 4
	SOURCE:0 = 3300
	SOURCE:7 = 2200
ELSE
	SOURCE:0 = 1500 + ABL:3 * 500
	SOURCE:7 = 2800
ENDIF

;奴隷のＰ⇔調教者の口の汚れが移動
STAIN:2 |= STAIN:PLAYER:0
STAIN:PLAYER:0 |= STAIN:2


IF TALENT:122 == 0 && TALENT:PLAYER:122 == 0
	PRINTS EXPNAME:40
	PRINTL +1
	EXP:40 += 1
	EXP:PLAYER:40 += 1
ELSEIF TALENT:122 == 1 && TALENT:PLAYER:122 == 1
	PRINTS EXPNAME:41
	PRINTL +1
	EXP:41 += 1
	EXP:PLAYER:41 += 1
ENDIF

;プレイヤーのABL:技巧をみる
IF ABL:PLAYER:2 == 0
	TIMES SOURCE:0 , 0.50
	TIMES SOURCE:7 , 0.50
ELSEIF ABL:PLAYER:2 == 1
	TIMES SOURCE:0 , 0.80
	TIMES SOURCE:7 , 0.80
ELSEIF ABL:PLAYER:2 == 2
	TIMES SOURCE:0 , 1.00
	TIMES SOURCE:7 , 1.00
ELSEIF ABL:PLAYER:2 == 3
	TIMES SOURCE:0 , 1.20
	TIMES SOURCE:7 , 1.50
ELSEIF ABL:PLAYER:2 == 4
	TIMES SOURCE:0 , 1.50
	TIMES SOURCE:7 , 2.50
ELSE
	SOURCE:0 = SOURCE:0 * (ABL:PLAYER:2 * 75 + 25) / 200
	SOURCE:7 = SOURCE:7 * (ABL:PLAYER:2 * 75 + 25) / 100
ENDIF

;ABL:奉仕精神をみる
IF ABL:PLAYER:6 == 0
	TIMES SOURCE:3 , 0.60
;	TIMES SOURCE:7 , 0.60
ELSEIF ABL:PLAYER:6 == 1
	TIMES SOURCE:3 , 0.80
;	TIMES SOURCE:7 , 0.80
ELSEIF ABL:PLAYER:6 == 2
	TIMES SOURCE:3 , 1.00
;	TIMES SOURCE:7 , 1.00
ELSEIF ABL:PLAYER:6 == 3
	TIMES SOURCE:3 , 1.50
;	TIMES SOURCE:7 , 1.50
ELSEIF ABL:PLAYER:6 == 4
	TIMES SOURCE:3 , 2.50
;	TIMES SOURCE:7 , 2.50
ELSE
	SOURCE:3 = SOURCE:3 * (ABL:PLAYER:2 * 75 + 25) / 100
;	SOURCE:7 = SOURCE:7 * (ABL:PLAYER:2 * 75 + 25) / 100
ENDIF

;主導権をチェック
IF TFLAG:45 == 0
	;主導権が主人公側
	;ABL:従順をみる
	IF ABL:0 <= 1
		SOURCE:21 += 300 + (ABL:0 * 50)
	ELSEIF ABL:0 <= 3
		SOURCE:21 += 600 + (ABL:0 * 100)
	ELSEIF ABL:0 <= 5
		SOURCE:21 += 800 + (ABL:0 * 120)
	ELSEIF ABL:0 <= 8
		SOURCE:21 += 1000 + (ABL:0 * 140)
	ELSEIF ABL:0 <= 11
		SOURCE:21 += 1200 + (ABL:0 * 160)
	ELSE
		SOURCE:21 += 1500 + (ABL:0 * 180)
	ENDIF
ELSE
	;主導権が相手側
	;ABL:サドっ気をみる
	IF ABL:15 == 0
		SOURCE:20 = 600
	ELSEIF ABL:15 == 1
		SOURCE:20 = 900
	ELSEIF ABL:15 == 2
		SOURCE:20 = 1200
	ELSEIF ABL:15 == 3
		SOURCE:20 = 1600
	ELSEIF ABL:15 == 4
		SOURCE:20 = 2000
	ELSE
		SOURCE:20 = 1000 + (ABL:15 * 300)
	ENDIF
ENDIF

;好感度ランダム変化
A = RAND:10 + 5
;合意がないと好感度が下がる
IF TALENT:152 == 0 && TFLAG:48 == 0
	A -= 9
	SOURCE:15 += 400
ENDIF
TFLAG:30 += A
TFLAG:37 += A / 2

RETURN 1



;パイズリする
@TRAIN_MESSAGE_COM192
PRINTFORM %CALLNAME:PLAYER%は
	IF TALENT:PLAYER:109
		PRINTFORM 薄い胸で懸命に%CALLNAME:TARGET%のペニスを
	ELSEIF TALENT:PLAYER:110
		PRINTFORM 豊満な胸で%CALLNAME:TARGET%のペニスを包み込んで
	ELSE
		PRINTFORM 胸で%CALLNAME:TARGET%のペニスをはさんで
	ENDIF
PRINTW 擦りあげている…

