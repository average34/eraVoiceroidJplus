@COM_ABLE185
;口移し
;直前にフェラするで射精させていないとダメ
;もしくはコンドーム精飲で精液を口に含んでいるのが条件
;CFLAG108
;COM_ABLE共通判定
CALL COM_ABLE_COMMON, SELECTCOM
SIF RESULT == 0
	RETURN 0
;ウフフ中じゃないと無理
SIF TFLAG:44 == 0
	RETURN 0
;触手調教中はダメ
SIF TEQUIP:90
	RETURN 0
;直前がフェラかコンドーム精飲(助手/マスター)じゃないとダメ
SIF (PREVCOM != 4) && (PREVCOM != 183) && (PREVCOM != 184)
	RETURN 0
;直前のコマンドがフェラの場合
IF PREVCOM == 4
	;直前のフェラするで射精してないとダメ
	SIF TFLAG:10 < 1
		RETURN 0
ELSEIF PREVCOM == 183
	;コンドーム精飲直後にレズっ気3以上の助手が調教者でないとダメ
	SIF (ASSIPLAY == 0) || (ABL:ASSI:9 < 3)
		RETURN 0
ENDIF
RETURN 1

@COM185
;口移し

PRINTL 口移し


;-------------------------------------------------
;実行できるかの判定
;-------------------------------------------------
CALL COM_ORDER_COMMON
SIF RESULT == 0
	RETURN 0

STR:0 = 口移し
CALL TRAIN_MESSAGE_B
;-------------------------------------------------
;実行決定
;-------------------------------------------------
;-------------------------------------------------
;射精ゲージチェック（ふたなり・オトコの場合）
;-------------------------------------------------
SIF TALENT:PLAYER:119 || TALENT:PLAYER:122  || TALENT:PLAYER:121
	BASE:PLAYER:2 += CALC_EJAC(SELECTCOM, PLAYER)

;-------------------------------------------------
;ソースの計算
;-------------------------------------------------
LOSEBASE:0 += 0
LOSEBASE:1 += 80

SOURCE:13 = 1500
SOURCE:14 = 500

;上のほうで計算した汚れデータ
SOURCE:8 = Y*80 + 100

;Ｍ感覚を設定している場合快Ｍソース等取得
SIF FLAG:62 & 4194304
	CALL SOURCE_M_COM185

;ABL:奉仕精神をみる
IF ABL:6 == 0
	SOURCE:4 = 420
	SOURCE:5 = 150
	TIMES SOURCE:8 , 4.00
ELSEIF ABL:6 == 1
	SOURCE:4 = 500
	SOURCE:5 = 300
	TIMES SOURCE:8 , 2.50
ELSEIF ABL:6 == 2
	SOURCE:4 = 580
	SOURCE:5 = 600
	TIMES SOURCE:8 , 1.50
ELSEIF ABL:6 == 3
	SOURCE:4 = 660
	SOURCE:5 = 900
	TIMES SOURCE:8 , 1.00
ELSEIF ABL:6 == 4
	SOURCE:4 = 740
	SOURCE:5 = 1500
	TIMES SOURCE:8 , 0.50
ELSE
	SOURCE:4 = 820
	SOURCE:5 = 2200
	TIMES SOURCE:8 , 0.10
ENDIF

PRINTFORML %EXPNAME:96%＋１
EXP:96 += 1
EXP:PLAYER:96 += 1
IF TALENT:PLAYER:151
	PRINTFORMW %CALLNAME:PLAYER%は【キス未経験】を失いました
	TALENT:PLAYER:151 = 0
ENDIF

IF TALENT:151
	TFLAG:600 = 1
	CALL KOJO_JUN
	TALENT:151 = 0
	PRINTFORMW %CALLNAME:TARGET%は【キス未経験】を失いました
	TFLAG:38 = 1
ENDIF

;主導権をチェック
IF TFLAG:45 == 0
	;主導権が主人公側
	;ABL:従順をみる
	IF ABL:0 <= 1
		SOURCE:21 += 300 + (ABL:0 * 50)
	ELSEIF ABL:0 <= 3
		SOURCE:21 += 600 + (ABL:0 * 100)
	ELSEIF ABL:0 <= 5
		SOURCE:21 += 800 + (ABL:0 * 120)
	ELSEIF ABL:0 <= 8
		SOURCE:21 += 1000 + (ABL:0 * 140)
	ELSEIF ABL:0 <= 11
		SOURCE:21 += 1200 + (ABL:0 * 160)
	ELSE
		SOURCE:21 += 1500 + (ABL:0 * 180)
	ENDIF
ELSE
	;主導権が相手側
	;ABL:サドっ気をみる
	IF ABL:15 == 0
		SOURCE:20 = 600
	ELSEIF ABL:15 == 1
		SOURCE:20 = 900
	ELSEIF ABL:15 == 2
		SOURCE:20 = 1200
	ELSEIF ABL:15 == 3
		SOURCE:20 = 1600
	ELSEIF ABL:15 == 4
		SOURCE:20 = 2000
	ELSE
		SOURCE:20 = 1000 + (ABL:15 * 300)
	ENDIF
ENDIF

;好感度ランダム変化
A = RAND:10 + 5
;合意がないと好感度が下がる
IF TALENT:152 == 0 && TFLAG:48 == 0
	A -= 3
	SOURCE:15 += 100
ENDIF
TFLAG:30 += A
TFLAG:37 += A / 2

;-------------------------------------------------
;射精チェック
;-------------------------------------------------
CALL SAMEN_CHECK
;S = BASE:PLAYER:2
;EJAC = MAXBASE:PLAYER:2
;
;IF  S > EJAC * 2
;	E = 2
;ELSEIF S > EJAC
;	E = 1
;ELSE
;	E = 0
;ENDIF

IF E
;射精している
	TIMES SOURCE:4 , 3.00

	;ABL:精液中毒をみる
	IF ABL:11 == 0
		SOURCE:7 = 0
		TIMES SOURCE:5 , 2.00
		TIMES SOURCE:13, 2.00
	ELSEIF ABL:11 == 1
		SOURCE:7 = 100
		TIMES SOURCE:5 , 2.50
		TIMES SOURCE:13, 1.60
	ELSEIF ABL:11 == 2
		SOURCE:7 = 300
		TIMES SOURCE:5 , 3.00
		TIMES SOURCE:13, 1.00
	ELSEIF ABL:11 == 3
		SOURCE:7 = 700
		TIMES SOURCE:5 , 4.00
		TIMES SOURCE:13, 0.70
	ELSEIF ABL:11 == 4
		SOURCE:7 = 1500
		TIMES SOURCE:5 , 5.00
		TIMES SOURCE:13, 0.40
	ELSE
		SOURCE:7 = (ABL:11 * 2000) - 4000
		SOURCE:5 = SOURCE:5 * (ABL:11 * 50 + 350) / 100
		TIMES SOURCE:13, 0.10
	ENDIF

	;射精先タイプの設定
	TFLAG:703 = 11
	;射精先選択の可・不可
	TFLAG:704 = 0
	;汎用的な処理はこの関数で行う
	;（射精ゲージの再計算や射精経験の上昇と共通部分の表示）
	CALL SAMEN_SHOOT
ENDIF

;;大量射精
;IF E == 2
;	TIMES SOURCE:7 , 2.00
;	TIMES SOURCE:5 , 1.50
;
;	EXP:PLAYER:3 += 2
;	EXP:20 += 1
;	PRINTL 大量射精
;	PRINTL 精液経験＋１
;	;精液汚れ
;	STAIN:PLAYER:2 |= 4
;
;	BASE:PLAYER:2 -= EJAC*2
;	SIF BASE:PLAYER:2 >= EJAC
;		BASE:PLAYER:2 = EJAC-1
;
;	;キスで射精させたフラグ
;	TFLAG:4 = 2
;;通常の射精
;ELSEIF E == 1
;	EXP:PLAYER:3 += 1
;	PRINTL 射精
;	;精液汚れ
;	STAIN:PLAYER:2 |= 4
;
;	BASE:PLAYER:2 -= EJAC
;	SIF BASE:PLAYER:2 >= EJAC
;		BASE:PLAYER:2 = EJAC-1
;
;	;キスで射精させたフラグ
;	TFLAG:4 = 1
;ENDIF


;調教者の口の汚れと奴隷の口の汚れを処理
S = STAIN:PLAYER:0
STAIN:PLAYER:0 |= STAIN:0
STAIN:0 |= S

IF TALENT:122 == 0 && TALENT:PLAYER:122 == 0
	PRINTS EXPNAME:40
	PRINTL +3
	EXP:40 += 3
	EXP:PLAYER:40 += 3
ELSEIF TALENT:122 == 1 && TALENT:PLAYER:122 == 1
	PRINTS EXPNAME:41
	PRINTL +3
	EXP:41 += 3
	EXP:PLAYER:41 += 3
ENDIF

;既成事実２に相当
TFLAG:200 = 2

RETURN 1

;Ｍ感覚を設定している場合快Ｍソース等取得
@SOURCE_M_COM185
;ABL:Ｍ感覚をみる
SOURCE:24 = 100 + (25 * ABL:17)

;EXP:キス経験をみる
IF EXP:96 < EXPLV:1
	TIMES SOURCE:24 , 1.00
ELSEIF EXP:96 < EXPLV:2
	TIMES SOURCE:14 , 0.95
	TIMES SOURCE:24 , 1.05
ELSEIF EXP:96 < EXPLV:3
	TIMES SOURCE:14 , 0.90
	TIMES SOURCE:24 , 1.10
ELSEIF EXP:96 < EXPLV:4
	TIMES SOURCE:14 , 0.85
	TIMES SOURCE:24 , 1.15
ELSEIF EXP:96 < EXPLV:5
	TIMES SOURCE:14 , 0.80
	TIMES SOURCE:24 , 1.20
ELSE
	TIMES SOURCE:14 , 0.75
	TIMES SOURCE:24 , 1.20
ENDIF

;ABL:精液中毒をみる
IF ABL:11 == 0
	TIMES SOURCE:24 , 0.80
ELSEIF ABL:11 == 1
	TIMES SOURCE:24 , 1.00
ELSEIF ABL:11 == 2
	TIMES SOURCE:24 , 1.20
ELSEIF ABL:11 == 3
	TIMES SOURCE:24 , 1.40
ELSEIF ABL:11 == 4
	TIMES SOURCE:24 , 1.60
ELSE
	TIMES SOURCE:24 , 1.80
ENDIF

;口移し
@TRAIN_MESSAGE_COM185
PRINTFORMW %CALLNAME:PLAYER%は口に含んだ精液を唾液と混ぜ、キスで口移しした……

;恋慕で精液中毒LV3以上でマスターの精液をマスターが飲ませる
IF TALENT:85 && (CFLAG:PLAYER:108 == 1) && (ABL:TARGET:11 >= 3 ) && (PLAYER == MASTER)
	PRINTFORMW %CALLNAME:TARGET%は嬉しそうに%CALLNAME:MASTER%の精液を味わっている…
	PRINTFORMW そして、名残惜しそうに、%CALLNAME:TARGET%と舌を絡め、口内を舐め回している…
;精液中毒5以上
ELSEIF ABL:TARGET:11 >= 5
	PRINTFORMW %CALLNAME:TARGET%は%CALLNAME:PLAYER%の唾液ごと貪欲に吸引し、
	PRINTFORMW さらに%CALLNAME:PLAYER%の口内を舐め回して残り香を愉しんでいる…
;精液中毒3以上
ELSEIF ABL:TARGET:11 >= 3
	PRINTFORM %CALLNAME:TARGET%は恍惚としながら、
	IF CFLAG:108 == 1
		PRINTS CALLNAME:MASTER
	ELSEIF CFLAG:108 == 2
		PRINTS CALLNAME:ASSI
	ELSE
		PRINT 己
	ENDIF
	PRINTFORMW の子種を%CALLNAME:PLAYER%の舌と絡め合っている…
;精液中毒1以上
ELSEIF ABL:TARGET:11 >= 1
	PRINTFORM %CALLNAME:TARGET%は
	IF CFLAG:108 == 1
		PRINTS CALLNAME:MASTER
	ELSEIF CFLAG:108 == 2
		PRINTS CALLNAME:ASSI
	ELSE
		PRINT 自分
	ENDIF
	PRINTFORM の精液を飲まされ、
	IF TALENT:85 && (CFLAG:108 == 1)
		PRINTFORMW 少し顔を赤らめている…
	ELSE
		PRINTFORMW 屈辱に満ちた表情をしている…
	ENDIF
;精液中毒なし
ELSE
	PRINTFORMW %CALLNAME:TARGET%は涙目で逃れようとする…
	PRINTFORMW しかし%CALLNAME:PLAYER%に追い詰められ、諦めたような表情で口内に精液を流し込まれている…
	PRINTFORM %CALLNAME:TARGET%は口内に溜まった精液を咽せながら
	SIF TALENT:85 && (CFLAG:108 == 1) && (PLAYER == MASTER)
		PRINT も懸命に
	PRINTFORMW 嚥下していく…
ENDIF

;-------------------------------------------------
;固有の実行判定
;-------------------------------------------------
@COM_ORDER_185
;;PLAYERが無理矢理するので実行はいつでも可能
;適当にキス辺りの処理を持ってくる

A = 0
S = 0
V = 85
;すべての命令に共通の要素を考慮
;(従順が高いと命令に従いやすいなど)
CALL COM_ORDER

;男嫌い
IF TALENT:82 && TALENT:PLAYER:122
	A -= 7
ENDIF

;ABL:欲望
IF ABL:1
	SIF S
	A += ABL:1 * 1
ENDIF
;ABL:奉仕精神
IF ABL:6
	SIF S
	A += ABL:6 * 4
ENDIF

IF MARK:1
	SIF S
	A += MARK:1 * 2
ENDIF


;PALAM:欲情
IF PALAM:5 < PALAMLV:1
	L = 0
ELSEIF PALAM:5 < PALAMLV:2
	L = 1
ELSEIF PALAM:5 < PALAMLV:3
	L = 2
ELSEIF PALAM:5 < PALAMLV:4
	L = 3
ELSEIF PALAM:5 < PALAMLV:5
	L = 4
ELSE
	L = 5
ENDIF
IF L
	SIF S
	A += L * 1
ENDIF

;恥じらい
IF TALENT:35
	A -= 5
ENDIF
;汚臭鈍感
IF TALENT:61
	SIF S
	A += 1
ENDIF
;汚臭敏感
IF TALENT:62
	A -= 3
ENDIF
;献身的
IF TALENT:63
	SIF S
	A += 6
ENDIF
;快感の否定
IF TALENT:71
	A -= 1
ENDIF
;恋慕
IF TALENT:85 && ASSIPLAY == 0
	SIF S
	A += 5
	S = 5
ENDIF

Y = 0
;愛液の汚れ
SIF STAIN:PLAYER:0 & 1
	Y += 1
;精液の汚れ
SIF STAIN:PLAYER:0 & 4
	Y += 3
;アナルの汚れ
SIF STAIN:PLAYER:0 & 8
	Y += 7
SIF STAIN:PLAYER:0 & 16
	Y += 15

SIF TALENT:61
	Y /= 3
SIF TALENT:62
	Y *= 2

;キスでは汚れがそれほど影響しない
Y /= 2

;汚れあり
IF Y
	A -= Y
	;汚臭鈍感
	IF TALENT:61
	ELSEIF TALENT:62
	ELSE
	ENDIF
ENDIF



;キス未経験
IF TALENT:151
	A -= 30
ENDIF

;一線越えない（キス未経験時のみ）
IF TALENT:151 && TALENT:27
	A -= 15
ENDIF


;合意
IF TALENT:152 == 0
	A -= 10
ENDIF


;好感度
IF CFLAG:2 < 500
	B = 0
	IF CFLAG:2 < 100
		B = 20
	ELSEIF CFLAG:2 < 300
		B = 10
	ELSE
		B = 5
	ENDIF
	A -= B
ENDIF

;好感度
IF CFLAG:2 >= 700
	B = 0
	SIF S
	B = CFLAG:2 / 80
	A += B
ENDIF

IF ((FLAG:23 & 1p27) == 0) && FLAG:2 != 1
	;男嫌い
	IF TALENT:82 && TALENT:PLAYER:122
		PRINT  - 
		PRINTS TALENTNAME:82
		PRINTV '(,7,')
		S = 1
	ENDIF

	;ABL:欲望
	IF ABL:1
		SIF S
			PRINT  + 
		PRINTS ABLNAME:1
		PRINTV 'LV,ABL:1,'(,ABL:1 * 1,')
		S = 1
	ENDIF
	;ABL:奉仕精神
	IF ABL:6
		SIF S
			PRINT  + 
		PRINTS ABLNAME:6
		PRINTV 'LV,ABL:6,'(,ABL:6 * 4,')
		S = 1
	ENDIF

	IF MARK:1
		SIF S
			PRINT  + 
		PRINTS MARKNAME:1
		PRINTV 'LV,MARK:1,'(,MARK:1 * 2,')
		S = 1
	ENDIF


	;PALAM:欲情
	IF PALAM:5 < PALAMLV:1
		L = 0
	ELSEIF PALAM:5 < PALAMLV:2
		L = 1
	ELSEIF PALAM:5 < PALAMLV:3
		L = 2
	ELSEIF PALAM:5 < PALAMLV:4
		L = 3
	ELSEIF PALAM:5 < PALAMLV:5
		L = 4
	ELSE
		L = 5
	ENDIF
	IF L
		SIF S
			PRINT  + 
		PRINTS PALAMNAME:5
		PRINTV 'LV,L,'(,L * 1,')
		S = 1
	ENDIF

	;恥じらい
	IF TALENT:35
		PRINT  - 
		PRINTS TALENTNAME:35
		PRINTV '(,5,')
		S = 1
	ENDIF
	;汚臭鈍感
	IF TALENT:61
		SIF S
			PRINT  + 
		PRINTS TALENTNAME:61
		PRINTV '(,1,')
		S = 1
	ENDIF
	;汚臭敏感
	IF TALENT:62
		PRINT  - 
		PRINTS TALENTNAME:62
		PRINTV '(,3,')
		S = 1
	ENDIF
	;献身的
	IF TALENT:63
		SIF S
			PRINT  + 
		PRINTS TALENTNAME:63
		PRINTV '(,6,')
		S = 1
	ENDIF
	;快感の否定
	IF TALENT:71
		PRINT  - 
		PRINTS TALENTNAME:71
		PRINTV '(,1,')
		S = 1
	ENDIF
	;恋慕
	IF TALENT:85 && ASSIPLAY == 0
		SIF S
			PRINT  + 
		PRINTS TALENTNAME:85
		PRINTV '(,5,')
		S = 5
	ENDIF

	Y = 0
	;愛液の汚れ
	SIF STAIN:PLAYER:0 & 1
		Y += 1
	;精液の汚れ
	SIF STAIN:PLAYER:0 & 4
		Y += 3
	;アナルの汚れ
	SIF STAIN:PLAYER:0 & 8
		Y += 7
	SIF STAIN:PLAYER:0 & 16
		Y += 15

	SIF TALENT:61
		Y /= 3
	SIF TALENT:62
		Y *= 2

	;キスでは汚れがそれほど影響しない
	Y /= 2

	;汚れあり
	IF Y
		PRINT  - 
		;汚臭鈍感
		IF TALENT:61
			PRINT 汚れあり、
			PRINTS TALENTNAME:61
		ELSEIF TALENT:62
			PRINT 汚れあり、
			PRINTS TALENTNAME:62
		ELSE
			PRINT 汚れあり
		ENDIF
		PRINTV '(,Y,')
		S = 1
	ENDIF



	;キス未経験
	IF TALENT:151
		PRINT  - 
		PRINTS TALENTNAME:151
		PRINTV '(,30,')
		S = 1
	ENDIF

	;一線越えない（キス未経験時のみ）
	IF TALENT:151 && TALENT:27
		PRINT  - 
		PRINTS TALENTNAME:27
		PRINTV '(,15,')
		S = 1
	ENDIF


	;合意
	IF TALENT:152 == 0
		PRINT  - 
		PRINTFORM %TALENTNAME:152%無し
		PRINTV '(,10,')
		S = 1
	ENDIF


	;好感度
	IF CFLAG:2 < 500
		B = 0
		PRINT  - 
		IF CFLAG:2 < 100
			B = 20
		ELSEIF CFLAG:2 < 300
			B = 10
		ELSE
			B = 5
		ENDIF
		PRINT 好感度不足
		PRINTV '(,B,')
		S = 1
	ENDIF

	;好感度
	IF CFLAG:2 >= 700
		B = 0
		SIF S
			PRINT  + 
		B = CFLAG:2 / 80
		PRINT 好感度
		PRINTV '(,B,')
		S = 1
	ENDIF


	;合計を表示(85以上で実行)
	PRINT  = 
	PRINTV A


	SIF A < V
		PRINT  < 
	SIF A == V
		PRINT  = 
	SIF A > V
		PRINT  > 
	PRINT 実行値
	PRINTV V
	WAIT
ENDIF

