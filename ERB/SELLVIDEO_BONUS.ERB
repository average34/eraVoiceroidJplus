;俺のジャスティスがないぞって人は手動で追加してください
;ないとは思いますが既存同人誌のタイトルとかぶっていたら代替案を添えて教えてください
;正直ネタが滑ってる気もしなくもない
;------------------------------------------------------------------
@VIDEO_BONUS
;ボーナスは一律$1000　今後変化の可能性アリ
;タイトルは組み合わせ優先
IF A:3 == 0
	IF TFLAG:32 & 1
		STR:0 = 処女モノ
	ELSE
		STR:0 = ハメ取り
	ENDIF
	
	IF ASSI > 0
		SIF ( TALENT:ASSI:恋慕 || TALENT:ASSI:淫乱 ) && ( TALENT:TARGET:恋慕 || TALENT:TARGET:淫乱 )
			CALL VIDEO_BONUS_XXYY
	ENDIF
	
	A:3 = 1
ENDIF

@VIDEO_BONUS_XXYY
IF 0
;月読兄妹
ELSEIF ( A:1 == 1 && A:2 == 2 ) || ( A:1 == 2 && A:2 == 1 )
	STR:0 = 禁断のショタ×ロリ近親相姦
	P += 1000
;ゆかマキ
ELSEIF ( A:1 == 3 && A:2 == 4 ) || ( A:1 == 4 && A:2 == 3 )
	STR:0 = 有名ゲーム実況者カップルの夜の情事
	P += 1000
;ずんきり
ELSEIF ( A:1 == 5 && A:2 == 10 ) || ( A:1 == 10 && A:2 == 5 )
	STR:0 = 姉妹丼　～ずんだときりたんぽ～
	P += 1000
;ずんタコ
ELSEIF ( A:1 == 5 && A:2 == 12 ) || ( A:1 == 12 && A:2 == 5 )
	STR:0 = ずん子のアイドルレッスン ～夜の部～
	P += 1000
;琴葉姉妹
ELSEIF ( A:1 == 6 && A:2 == 7 ) || ( A:1 == 7 && A:2 == 6 )
	STR:0 = コトノハメ取り！
	P += 1000
;ARIA姉妹
ELSEIF ( A:1 == 53 && A:2 == 54 ) || ( A:1 == 54 && A:2 == 53 )
	STR:0 = ＩＡ×ＯИＥ -ARIA ON DEMAND-
	P += 1000
;追加したい人用のテンプレ
;	;＆
;ELSEIF ( A:1 ==  && A:2 ==  ) || ( A:1 ==  && A:2 ==  )
;	STR:0 = 
;	P += 1
ENDIF
