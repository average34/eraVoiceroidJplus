;手動能力アップ
@ABL_MAN
PRINTFORML 珠を消費して%CALLNAME:TARGET%の能力アップができます。
CALL SHOW_JUEL
CALL SHOW_ABLUP_SELECT
INPUT

IF RESULT == 100
	RETURN 1
ELSEIF RESULT == 200
	CALL AUT_LVUPPER(RESULT)
ELSEIF RESULT == 300
	CALL AUT_LVUPPER(RESULT)
ELSE
	TRYCALLFORM ABLUP{RESULT}
ENDIF

RESTART
